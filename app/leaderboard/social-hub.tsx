"use client";

import { useMemo, useState } from "react";
import { BellOff, CalendarClock, Check, Flag, HandHeart, LockKeyhole, MessageCircleHeart, Plus, ShieldCheck, Sparkles, Timer, UserPlus, Users, X } from "lucide-react";
import { contributeToBoss, createFocusSession, privateVisibility, recordCommitment, sendEncouragement, transitionConnection, type BossState, type EncouragementKind, type VisibilityCategory } from "./social-engine";

type SocialView = "Allies" | "Guilds" | "Cooperative Bosses";
const visibilityLabels: Record<VisibilityCategory, string> = { habits: "Habit summaries", campaigns: "Campaign names", milestones: "Shared milestones", focus_sessions: "Focus availability", achievements: "Achievements", summary_metrics: "Progress summaries" };

export function SocialHub({ view }: { view: SocialView }) {
  const [optedIn, setOptedIn] = useState(false);
  if (!optedIn) return <section className="social-consent game-panel"><LockKeyhole size={30} /><p>PRIVATE SOCIAL LAYER</p><h2>Your circle starts closed</h2><span>Allies, guilds, sessions, and comparisons are invitation-only. Nothing from your profile or goals is shared until you choose a person and category.</span><ul><li><Check size={14} /> Notes, journals, evidence, calendar details, energy, and AI conversations stay private</li><li><Check size={14} /> Missing work never lowers anyone&apos;s progress or boss contribution</li><li><Check size={14} /> Block and report controls are always available</li></ul><button onClick={() => setOptedIn(true)}><ShieldCheck size={15} /> Opt in to trusted social</button><small>You can leave social mode at any time. Personal progress remains available.</small></section>;
  if (view === "Allies") return <Allies />;
  if (view === "Guilds") return <Guilds />;
  return <Bosses />;
}

function Allies() {
  const [connection, setConnection] = useState<"pending" | "accepted" | "declined" | "blocked" | "removed">("pending");
  const [rules, setRules] = useState(privateVisibility);
  const [notice, setNotice] = useState("Invite pending — nothing is visible yet.");
  const [minutes, setMinutes] = useState(25);
  const [intention, setIntention] = useState("Finish the first draft without switching contexts");
  const [session, setSession] = useState<ReturnType<typeof createFocusSession> | null>(null);
  const act = (action: "accept" | "decline" | "remove" | "block") => { const next = transitionConnection(connection, action); setConnection(next.state); if (next.state === "blocked") setRules(privateVisibility()); setNotice(next.state === "accepted" ? "Ally accepted. Choose each category you want to share." : next.state === "blocked" ? "Blocked. Mutual visibility and notifications were removed immediately." : `Connection ${next.state}.`); };
  const react = (kind: EncouragementKind) => { try { sendEncouragement({ kind, blocked: connection === "blocked", recentCount: 1 }); setNotice(`${kind.replace("_", " ")} sent — no points attached.`); } catch (error) { setNotice((error as Error).message); } };
  return <div className="social-grid"><section className="social-card game-panel"><header><div><p>ALLY INVITE / PRIVATE</p><h2>Mira Sol</h2></div><span className={`social-status ${connection}`}>{connection}</span></header><p className="social-copy">Mind Cartographer · invited you to a quiet accountability circle.</p>{connection === "pending" && <div className="social-actions"><button className="primary" onClick={() => act("accept")}><Check size={14} /> Accept</button><button onClick={() => act("decline")}><X size={14} /> Decline</button></div>}{connection === "accepted" && <><h3>Visible to Mira</h3><div className="visibility-list">{Object.entries(visibilityLabels).map(([key, label]) => <label key={key}><span>{label}<small>{rules[key as VisibilityCategory] ? "Shared with this ally" : "Private"}</small></span><input type="checkbox" checked={rules[key as VisibilityCategory]} onChange={(event) => setRules((current) => ({ ...current, [key]: event.target.checked }))} /></label>)}</div><div className="reaction-row"><button onClick={() => react("cheer")}><Sparkles size={14} /> Cheer</button><button onClick={() => react("respect")}><HandHeart size={14} /> Respect</button><button onClick={() => react("well_done")}><MessageCircleHeart size={14} /> Well done</button></div><div className="danger-actions"><button onClick={() => act("remove")}>Remove ally</button><button onClick={() => act("block")}><BellOff size={14} /> Block</button><button onClick={() => setNotice("Report received for private review. Contact is not encouraged.")}><Flag size={14} /> Report</button></div></>}</section><section className="social-card pact-card game-panel"><header><div><p>ACCOUNTABILITY PACT</p><h2>Portfolio launch</h2></div><span className="social-status accepted">2 allies</span></header><dl><div><dt>Purpose</dt><dd>Publish one honest case study</dd></div><div><dt>Window</dt><dd>14–28 June</dd></div><div><dt>Cadence</dt><dd>Mon / Thu check-in</dd></div><div><dt>Visibility</dt><dd>Members only</dd></div></dl><article><Users size={16} /><div><b>Your commitment</b><span>Draft the problem and outcome sections</span></div><button onClick={() => { const result = recordCommitment("self", "missed"); setNotice(result.recoveryPrompt ?? "Check-in saved."); }}>Need recovery</button></article><small>A missed check-in stays private and changes no XP, health, rank, reward, or ally progress.</small></section><section className="social-card focus-card game-panel"><header><div><p>SHARED FOCUS / QUIET</p><h2>Schedule a session</h2></div><Timer size={20} /></header>{!session ? <><div className="duration-row">{[25, 50, 75].map((value) => <button key={value} className={minutes === value ? "active" : ""} onClick={() => setMinutes(value)}>{value} min</button>)}</div><label>Starting intention<textarea value={intention} onChange={(event) => setIntention(event.target.value)} maxLength={240} /></label><button className="primary full" onClick={() => setSession(createFocusSession({ startsAt: new Date(Date.now() + 3600000).toISOString(), durationMinutes: minutes, intention }))}><CalendarClock size={15} /> Schedule private session</button></> : <div className="quiet-state"><span><Timer size={18} /> QUIET SESSION READY</span><strong>{session.durationMinutes}:00</strong><p>{session.intention}</p><button onClick={() => setSession(null)}>End with optional text check-in</button><small>Attendance XP: +{session.attendanceXp}. Only separately verified quest work can progress.</small></div>}</section><div className="social-notice" role="status"><ShieldCheck size={14} />{notice}</div></div>;
}

function Guilds() {
  const [joined, setJoined] = useState(true);
  const [notice, setNotice] = useState("Only invited members can see this guild.");
  return <div className="guild-layout"><section className="social-card guild-card game-panel"><header><div><p>PRIVATE GUILD / MEMBER</p><h2>Quiet Builders</h2></div><LockKeyhole size={20} /></header><p className="social-copy">A trusted room for shipping meaningful work without performance pressure.</p><div className="member-stack"><span>F</span><span>M</span><span>A</span><b>3 / 8 members</b></div><dl><div><dt>Owner</dt><dd>Mira Sol</dd></div><div><dt>Your role</dt><dd>Member</dd></div><div><dt>Discoverability</dt><dd>Invitation only</dd></div></dl><h3>Shared rules</h3><ul><li>Encourage the person, never score their struggle.</li><li>Ask before sharing summaries outside the guild.</li><li>No unsolicited direct messages.</li></ul>{joined ? <div className="social-actions"><button className="primary" onClick={() => setNotice("Invite link created for one trusted person; it expires in 24 hours.")}><UserPlus size={14} /> Invite ally</button><button onClick={() => { setJoined(false); setNotice("You left. Historical boss attribution remains, and future access is removed."); }}>Leave guild</button></div> : <button disabled>Access removed</button>}</section><aside className="social-card game-panel"><p>ROLE BOUNDARIES</p><h2>Clear authority</h2><div className="role-list"><article><b>Owner</b><span>Guild settings, admin roles, and closure</span></article><article><b>Admin</b><span>Invites, rules, and member safety</span></article><article><b>Member</b><span>Own sharing, pacts, focus, and reactions</span></article></div><small>No public discovery, stranger matching, or unrestricted messaging.</small></aside><div className="social-notice" role="status"><ShieldCheck size={14} />{notice}</div></div>;
}

function Bosses() {
  const initial = useMemo<BossState>(() => ({ id: "boss-1", target: 100, total: 68, settled: false, contributions: { self: 28, mira: 24, ari: 16 }, eventIds: ["q-1", "q-2", "q-3"] }), []);
  const [boss, setBoss] = useState(initial);
  const [notice, setNotice] = useState("Only eligible verified quest completions can contribute.");
  const contribute = () => { const result = contributeToBoss(boss, { participantId: "self", eventId: crypto.randomUUID(), verified: true, amount: 12, participantCap: 48 }); setBoss(result.boss); setNotice(result.rewardSettled ? "Objective cleared. Rewards settled exactly once by the server." : result.applied ? `Permanent contribution added: +${result.appliedAmount}.` : "Contribution cap reached; no change applied."); };
  return <div className="boss-layout"><section className="coop-boss game-panel"><header><div><p>COOPERATIVE OBJECTIVE / QUIET BUILDERS</p><h2>Launch the Signal</h2></div><span>{boss.settled ? "CLEARED" : "ACTIVE"}</span></header><div className="boss-core"><div><small>SHARED OBJECTIVE</small><h3>Publish three portfolio case studies as a guild</h3><p>Progress only moves forward. Nobody can damage this encounter by missing work.</p></div><strong>{boss.total}<small>/ {boss.target} SIGNAL</small></strong></div><div className="boss-meter" aria-label={`${boss.total} of ${boss.target} cooperative progress`}><i style={{ width: `${Math.min(100, boss.total / boss.target * 100)}%` }} /></div><div className="contribution-list">{Object.entries(boss.contributions).map(([id, amount]) => <article key={id}><span>{id === "self" ? "F" : id[0].toUpperCase()}</span><div><b>{id === "self" ? "You" : id === "mira" ? "Mira" : "Ari"}</b><small>Permanent verified contribution</small></div><strong>+{amount}</strong></article>)}</div><button className="primary boss-action" disabled={boss.settled} onClick={contribute}><Plus size={15} /> Simulate verified quest contribution</button></section><aside className="social-card reward-card game-panel"><Sparkles size={22} /><p>ONE-TIME SETTLEMENT</p><h2>Shared signal cache</h2><span>Everyone who reaches their private contribution threshold receives the cooperative badge. Lower contributions are never displayed as failure.</span><dl><div><dt>Your threshold</dt><dd>20 / 20 met</dd></div><div><dt>Encounter cap</dt><dd>48 per member</dd></div><div><dt>Settlement</dt><dd>{boss.settled ? "Recorded once" : "Pending objective"}</dd></div></dl><small>Leaving preserves historical attribution while revoking future encounter access.</small></aside><div className="social-notice" role="status"><ShieldCheck size={14} />{notice}</div></div>;
}
